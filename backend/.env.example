# Server Configuration
PORT=3001
JWT_SECRET=change-this-secret-in-production

# Application URL (used for password reset links)
APP_URL=http://localhost:3001

# Upload Configuration
MAX_FILE_SIZE_MB=500
UPLOAD_DIR=./storage/uploads

# Search Providers (at least one required for search feature)
# SearXNG - Self-hosted search engine (recommended)
SEARXNG_URL=

# Serper API - Google Search API (fallback)
SERPER_API_KEY=

# SMTP Configuration (for password recovery emails)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=Infra Study Lab <noreply@local>
